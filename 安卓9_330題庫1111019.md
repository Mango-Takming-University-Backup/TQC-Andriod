## 當設定相關性參數後，模組組態檔build.gradle的dependencies資料內容會變動。下列哪些項目是屬於正確的相關性設定值？

1. [x] compile 'com.android.support:appcompat-v7:23.0.1'
2. [x] compile project(':MyUtilities')
3. [x] compile files('libs/json.jar')
4. [x] compile fileTree(dir:'libs',include:['*.jar'])

---

## 下列哪一項是Android platform提供使用的Database？

1. [ ] MySQL
2. [ ] PostgreSQL
3. [ ] Oracle
4. [x] SQLite

---

## 關於系統預設產生的除錯金鑰檔案庫，下列敘述哪一項錯誤？

1. [ ] 除錯用的金鑰有效期限通常是一年
2. [ ] 除錯金鑰檔案庫若被刪除，系統會自動產生一個新的金鑰檔案庫，一年內有效
3. [x] 除錯用的金鑰沒有期限的限制，可以無限期使用
4. [ ] 以除錯金鑰進行簽章的App可以安裝到模擬器與一般實體手機

---

## 使用MediaPlayer播放音樂或影片時，可使用MediaPlayer.setDataSource()來設定要播放的Source，下列哪一項不是setDataSource()支援的建構方法？

1. [ ] MediaPlayer.setDataSource(String path);
2. [ ] MediaPlayer.setDataSource(FileDescriptor fd);
3. [x] MediaPlayer.setDataSource(FileInputStream stream);
4. [ ] MediaPlayer.setDataSource(Context context, Uri uri);

---

## 在Android SDK中，提供下列哪些工具來協助開發者做測試？

1. [x] monitor
2. [x] adb
3. [ ] mksdcard
4. [ ] zipalign

---

## 關於模擬器的設定，下列哪些正確？

1. [x] 可以自行設定螢幕尺寸
2. [x] 可以自行設定螢幕像素
3. [x] 可以自行設定記憶體大小
4. [x] 可以自行設定SD卡

---

## 繼承ContentProvider時，下列哪些方法必須自行實作？

1. [x] onCreate
2. [x] getType
3. [ ] drop
4. [ ] openFile

---

## 利用Gradle設定組態定義檔時，可以利用Flavors和Build Types指定不同的編譯組合方式，讓程式開發過程簡便不少，請問下列哪些是Build Types內建的選項？

1. [ ] normal
2. [x] debug
3. [ ] test
4. [x] release

---

## 在Android Studio新建專案的過程中，下列敘述哪些正確？

1. [ ] 「Layout Name」欄位預設值為activity_main，為活動名稱
2. [x] 「Company Domain」欄位值在自動組合後將會倒置為套件名稱
3. [x] 「Project location」欄位是指專案將儲存的檔案目錄路徑
4. [x] 「Minimum SDK」欄位是指此Android Project最低可支援的Android API程式函式庫版本

---

## 若要在res目錄中定義程式放入不需要壓縮的原始資料檔，例如：視訊檔，需在下列哪一項進行設定？

1. [x] raw
2. [ ] menu
3. [ ] color
4. [ ] values

---

## 在Activity運作中按下[BACK]鍵，Activity會最先呼叫下列哪一項方法？

1. [ ] onDestroy
2. [ ] onStop
3. [ ] onStopCommand
4. [x] onPause

---

## 在Activity運作中按下[HOME]鍵，Activity會最先呼叫下列哪一項方法？

1. [ ] onDestroy
2. [ ] onStop
3. [ ] onStopCommand
4. [x] onPause

---

## 下列哪一項方法，可從一個Activity跳轉到另一個Activity（需返回資訊）？

1. [ ] startActivity
2. [ ] startActivityByResult
3. [x] startActivityForResult
4. [ ] newActivity

---

## 關於版面配置的敘述，下列哪一項錯誤？

1. [ ] 可以透過程式直接定義版面配置
2. [ ] 在版面配置檔案中，以<?layout標籤作開頭
3. [ ] 可以使用的Layout包含LinearLayout、TableLayout
4. [x] 在LinearLayout元件中，可以透過android:port屬性定義螢幕方向

---

## 下列哪一項內建介面元件，設計目的為讓使用者能選擇多個選項？

1. [ ] RadioButton
2. [x] CheckBox
3. [ ] Gallery
4. [ ] ListBox

---

## 如果要開發Android Things的應用程式，Android SDK最低版本下列哪一項正確？

1. [ ] Android 7（Nougat），API 24
2. [ ] Android 7.1（Nougat），API 25
3. [ ] Android 8（Oreo），API 26
4. [x] Android 8.1（Oreo），API 27

---

## Activity在手機執行階段，其可視性（Visible）週期，分別起始與結束於兩個階段，下列哪一項正確？

1. [ ] Activity onRestore與Activity onStop
2. [x] Activity onStart與Activity onStop
3. [ ] Activity onResume與Activity onPause
4. [ ] Activity onCreate與Activity onDestroy

---

## 在網路安全性設定功能，要自訂信任的憑證伺服器（Certificate Authority, CA），需要在下列安全性設定檔的哪些標籤下進行設定？

1. [x] <base-config>
2. [ ] <trust-anchors>
3. [ ] <certificates>
4. [x] <domain-config>

---

## Room提供了一個SQLite之上的抽象層，提供開發者更方便的方式操作資料庫，而其主要的元件下列哪些正確？

1. [x] database
2. [x] entity
3. [x] DAO（Data Access Object）
4. [ ] table

---

## 關於Resources.openRawResource(R.raw.myDataFile)的敘述，下列哪些正確？

1. [x] 可以取得專案目錄下的res/raw/myDataFile
2. [ ] 可以取得/data/data/package_name/files/myDataFile
3. [x] 可以用來取得drawable、sound和raw resources
4. [ ] 可以透過取得的物件，將資料寫回myDataFile

---

## 在ImageView中設定一張動態的GIF圖檔，下列哪一項執行結果正確？

1. [ ] ImageView中不會顯示任何影像
2. [ ] 執行時會出現程式錯誤
3. [x] ImageView中僅會顯示動態GIF圖檔的第一個影像畫格
4. [ ] ImageView中可正常顯示GIF圖檔並可顯示動畫

---

## 子母畫面（Picture-in-Picture, PIP）模式讓應用程式在釘選視窗中執行影片活動，同時讓另一個活動在背景持續進行。如要宣告支援子母畫面的話，下列哪些配置參數需要設定為true？

1. [x] android:resizeableActivity
2. [x] android:supportsPictureInPicture
3. [ ] android:inMultiWindowAndroid
4. [ ] android:inPictureInPicture

---

## 下列哪一項內建介面元件，設計目的為讓使用者能輸入內容？

1. [ ] TextView
2. [x] EditText
3. [ ] CheckBox
4. [ ] ImageView

---

## 關於[HOME]鍵的敘述，下列哪一項正確？

1. [x] 按下[HOME]鍵會停止目前的Activity並將目前的Activity移到背景
2. [ ] 按下[HOME]鍵會停止目前的Activity並將目前的Activity移到前景
3. [ ] 按下[HOME]鍵會繼續目前的Activity並將目前的Activity移到前景
4. [ ] 按下[HOME]鍵會繼續目前的Activity並將目前的Activity移到背景

---

## 為了實現IPC（Interprocess Communication），在Android中需使用下列哪一項工具？

1. [ ] AVD
2. [ ] ADB
3. [x] AIDL
4. [ ] AAPT

---

## 要為自己的應用程式新增一個SearchManager的搜尋功能，下列敘述哪一項錯誤？

1. [ ] 需要一個Activty處理搜尋動作
2. [ ] 搜尋Activity會取得Intent.ACTION_SEARCH和查詢內容
3. [ ] 需要設定AndroidManifest.xml
4. [x] searchable.xml只有在需要公開給Quick Search Box時才需要設定

---

## 如附圖所示之程式碼，為程式使用VideoView的宣告，其記憶卡的根目錄存放了1.3gp影片檔，下列敘述哪一項正確？

1. [ ] 播放影片1.3gp，尺寸為原尺寸
2. [ ] 播放影片1.3gp，放大尺寸
3. [ ] 程式執行後，播放顯示黑色的畫面
4. [x] 程式有錯，無法執行

---

## 所有的介面元件都繼承自下列哪一項類別？

1. [x] View
2. [ ] Widget
3. [ ] Interface
4. [ ] UI

---

## 在應用程式中可使用BluetoothAdapter.getDefaultAdapter()來取得BluetoothAdapter物件，請問要判斷手持設備是否開啟藍牙，要用下列哪一個方法？

1. [ ] BluetoothAdapter.isOpened()
2. [x] BluetoothAdapter.isEnabled()
3. [ ] BluetoothAdapter.enabled()
4. [ ] BluetoothAdapter.opened()

---

## 若要利用Google Play Service進行程式開發，需利用Android SDK Manager進行下載、安裝。在開啟SDK Manager和點選Extras之後，下列哪些項目一定要勾選並下載？

1. [ ] Android Support Repository
2. [x] Google Repository
3. [ ] Android Support Library
4. [x] Google Play Service

---

## 在應用程式中若要使用WifiManager來掃描附近Wi-Fi熱點時，要呼叫下列哪一個方法？

1. [x] startScan()
2. [ ] scan()
3. [ ] scanWifi()
4. [ ] start()

---

## 下列哪一項類別可攜帶資料，從一個Activity傳送資料到另一個Activity？

1. [ ] Service
2. [x] Bundle
3. [ ] Data
4. [ ] Info

---

## Activity的生命週期中，包括下列哪些方法？

1. [x] onResume
2. [x] onPause
3. [ ] onClose
4. [x] onCreate

---

## 若應用程式會使用藍牙功能，並操作藍牙設置，在建置Android Bluetooth Low Energy（BLE）應用程式時，需加入下列哪些權限與特性聲明？

1. [x] android.permission.BLUETOOTH
2. [x] android.permission.BLUETOOTH_ADMIN
3. [x] android.hardware.bluetooth_le
4. [ ] 不需加入任何權限與聲明設定

---

## 如果需要讓資料在多個應用程式間共享，應該使用下列哪一項方式？

1. [x] Content Providers
2. [ ] 使用同一個檔案，透過檔案存取方式分享
3. [ ] 使用同一個資料庫檔案，直接在應用程式內連接該檔案
4. [ ] 應用程式間使用網路連線的方式共享

---

## 下列哪一個樣式（Theme）的參數設定，可以讓Android應用程式（App）使用系統桌面作為應用程式背景？

1. [ ] android:theme = "Theme.Light"
2. [ ] android:theme = "Translucent"
3. [ ] android:theme = "Theme.Panel"
4. [x] android:theme = "Theme.Wallpaper"

---

## 如果要使用JobService來執行服務，繼承JobService的類別，在清單檔案宣告參數中需要增加下列哪一項permission？

1. [x] android.permission.BIND_JOB_SERVICE
2. [ ] android.permission.BIND_DEVICE_ADMIN
3. [ ] android.permission.BIND_ACCESSIBILITY_SERVICE
4. [ ] android.permission.BIND_NOTIFICATION_LISTENER_SERVICE

---

## 將應用程式發佈至Google Play時，系統支援的單一APK檔案大小上限為下列哪一個大小？

1. [ ] 40MB
2. [x] 50MB
3. [ ] 60MB
4. [ ] 沒有限制

---

## 若要瀏覽和管理App專案中的檔案，下列哪一項正確？

1. [x] 專案檢視視窗
2. [ ] App畫面預覽視窗
3. [ ] 模擬器狀態視窗
4. [ ] 除錯視窗

---

## 為了整合Google相關的應用服務，程式必須利用Google Play Service Client API與Google Play Service底層溝通；但是，設備端（手機/平板）有最低版本的要求。下列哪一項是最低要求的版本？

1. [ ] Android 1.6（API level 4）
2. [ ] Android 2.1（API level 7）
3. [x] Android 2.3（API level 9）
4. [ ] Android 5.0（API level 21）

---

## 如附圖所示之程式碼，需在行號07的區塊加入下列哪一段程式，才會畫出如附圖的「空心圓形」？

1. [ ] ^[GD90505]
2. [ ] ^[GD90506]
3. [x] ^[GD90507]
4. [ ] ^[GD90508]

---

## 若要進行單元測試，需在下列哪一個介面中新增配置？

1. [ ] Version Control
2. [x] Run/Debug Configurations
3. [ ] AVD Manager
4. [ ] SDK Manager

---

## 在實作撥號相關的Android應用程式時，可以透過PhoneStateListener來監控手機電話狀態，並覆寫onCallStateChanged()方法，透過傳入的state來判斷電話狀態。其中state等於下列哪一項選項時，代表有電話打進來？

1. [x] TelephonyManager.CALL_STATE_RINGING
2. [ ] TelephonyManager.CALL_STATE_IDLE
3. [ ] TelephonyManager.CALL_STATE_ONLINE
4. [ ] TelephonyManager.CALL_STATE_OFFHOOK

---

## TelephonyManager.getSimState()方法可以取得手機SIM卡的狀態，請問如果手機內無SIM卡時，getSimState()方法的回傳值為下列哪一項選項？

1. [x] TelephonyManager.SIM_STATE_ABSENT
2. [ ] TelephonyManager.SIM_STATE_UNKNOWN
3. [ ] TelephonyManager.SIM_STATE_PUK_REQUIRED
4. [ ] TelephonyManager.SIM_STATE_NETWORK_LOCKED

---

## 在應用程式中可使用WifiManager來管理手持設備的無線網路連線。請問下列哪一個方法可以用來關閉手持設備的無線網路連線？

1. [ ] setWifiDisabled(true)
2. [x] setWifiEnabled(false)
3. [ ] setWifiDisabled()
4. [ ] setWifiClosed()

---

## 下列哪些指令具有註解的功能？

1. [ ] [Ctrl] + [F]
2. [x] [Ctrl] + [/]
3. [ ] [Ctrl] + [Shift] + [F]
4. [x] [Ctrl] + [Shift] + [/]

---

## AnimationDrawable可以製作出如動態GIF圖檔的動畫效果，可將設定檔預先設定成XML檔案，並儲存於/res資料夾下。關於AnimationDrawable的敘述，下列哪一項錯誤？

1. [ ] AnimationDrawable.start()可啟動動畫運行
2. [ ] 在XML設定檔中定義android:oneshot="true"可使AnimationDrawable執行時只執行一個循環
3. [x] android:oneshot屬性的預設值為true
4. [ ] AnimationDrawable.stop()可停止動畫運行

---

## 關於Gradle模組組態設定中的參數，下列敘述哪一項錯誤？

1. [ ] applicationId是用來定義App唯一的識別碼，通常會與套件名稱相同
2. [ ] minSdkVersion是指定可執行該App的最低Android版本
3. [ ] targetSdkVersion是指定可執行該App的標準Android版本
4. [x] maxSdkVersion是指定可執行該App的最低Android版本

---

## 關於群組通知（Groups Of Notifications）的敘述，下列哪些正確？

1. [x] 通過群組通知，可以確保不向用戶顯示冗餘數據
2. [x] 將多個通知整合在一個通知中，也可以展開看所包含的所有通知
3. [ ] Wear OS設備不支援Bundled Notifications
4. [ ] 此消息佔據一個矩形框，一段時間後會淡入並淡出。框的大小取決於消息內容

---

## 若要對Android Studio修改預設的設定，例如：編碼、版本控管、語言等等，可以在下列哪一項進行設定？

1. [x] File/Settings
2. [ ] File/Project Structure
3. [ ] File/Import Settings
4. [ ] File/Export Settings

---

## 在Android Things系統服務中，下列哪一項是負責管理外部設備連接的系統服務？

1. [ ] LocationManager
2. [ ] ServiceManager
3. [x] PeripheralManager
4. [ ] BluetoothManager

---

## Instant Native Android apps與一般需安裝的App，在應用上有下列哪些差別或限制？

1. [x] 無法使用背景服務
2. [x] 無法執行背景通知
3. [x] 無法存取裝置外部儲存空間
4. [ ] 沒有任何差別或限制

---

## 在省電模式中，使用Doze模式可能會影響AlarmManager警報和計時器管理，可使用下列哪些方法讓系統處於Doze模式下也會觸發警報？

1. [x] setAndAllowWhileIdle()
2. [ ] setAlarmClock()
3. [ ] setExact()
4. [x] setExactAndAllowWhileIdle()

---

## Android預設會將openOutputFile的檔案存放在下列哪一項目錄下？

1. [x] /data/data/package_name/files
2. [ ] /data/app/package_name/files
3. [ ] /data/app/package_name/shared_pref
4. [ ] /data/app/package_name/databases

---

## 關於Toast的敘述，下列哪一項正確？

1. [ ] 可以用來建立對話框
2. [ ] 可以在其中加入按鈕
3. [x] 可以在不中斷正常操作的情況下顯示提示訊息
4. [ ] 可以指定其顯示時間（秒數）

---

## 關於LinearLayout版面配置元件的敘述，下列哪一項錯誤？

1. [ ] LinearLayout元件中，可以包含多層版面
2. [x] 在LinearLayout元件中，可以透過android:width屬性定義內容元件寬度
3. [ ] 可以透過getOrientation方法，取得LinearLayout元件的排列方向
4. [ ] 在LinearLayout元件中，可以透過android:orientation屬性定義內容元件排列方向

---

## 關於Broadcast Receiver的敘述，下列哪一項正確？

1. [ ] Broadcast Intent是在前景運行的動作
2. [ ] Broadcast Intent後會啟動對應的Activity
3. [x] 可透過<receiver>標籤在AndroidManifest.xml中註冊
4. [ ] 使用者可以直觀感受到Broadcast的效果

---

## 如附圖所示，下列哪一項是用來群組化設定項目，以方便使用者檢視的元件？

1. [x] PreferenceCategory
2. [ ] PreferenceScreen
3. [ ] RingtonePreference
4. [ ] ListPreference

---

## 重新啟動一個Activity時，下列哪一項是正確的生命週期順序？

1. [x] onCreate → onStart → onResume
2. [ ] onStop → onResume → onCreate
3. [ ] onResume → onStart → onOpen
4. [ ] onPause → onStart → onResume

---

## 在openFileOutput()時使用MODE_APPEND，會產生下列哪些結果？

1. [x] 在檔案內繼續添加內容而非覆蓋
2. [x] 該檔案可以立即被寫入內容
3. [x] 如果呼叫時該檔案不存在，會丟出例外
4. [ ] 產生的檔案可以被其他應用程式存取

---

## 關於FCM（Firebase Cloud Message）的敘述，下列哪些正確？

1. [x] 每一則FCM發送的大小上限為4KB
2. [x] 訊息的存活最長時間為四週
3. [ ] 伺服器端發送FCM後，FCM SERVER不會回覆發送狀況
4. [ ] 伺服器發送FCM需要收費

---

## 要撰寫一個包含中英文的多語系應用程式，可以在專案res目錄中加入下列哪些目錄來達成？

1. [x] values目錄
2. [ ] values_zh目錄
3. [x] values-zh-rTW目錄
4. [ ] values-zh_rTW目錄

---

## 關於應用程式發佈至Google Play的敘述，下列哪些正確？

1. [ ] APK不需要憑證簽署
2. [x] 套件名稱（package name）不可與其他應用程式重複
3. [x] 使用的圖片不可有侵害版權的情形
4. [ ] 主題圖片可以不用上傳

---

## 如果要建立可以隨著應用程式（App）被移除而一同被移除的外部儲存（External Storage）檔案，那麼外部儲存檔案的儲存路徑中一定要包含下列哪一個名稱？

1. [ ] 應用程式名稱（Application Name）
2. [ ] 活動名稱（Activity Name）
3. [x] 套件名稱（Package Name）
4. [ ] 專案名稱（Project Name）

---

## 在Android應用程式中要實作照相機的功能，需在AndroidManifest.xml設定檔中加入下列哪一項權限設定？

1. [ ] <uses-permission android:name="android.permission.PICTURE"/>
2. [ ] <uses-permission android:name="android.permission.REORDER_TASKS"/>
3. [x] <uses-permission android:name="android.permission.CAMERA"/>
4. [ ] 不需加入任何權限設定

---

## 下列哪一項不是Android支援的Sensor類型？

1. [ ] Sensor.TYPE_GYROSCOPE
2. [ ] Sensor.TYPE_LIGHT
3. [x] Sensor.TYPE_MOVE
4. [ ] Sensor.TYPE_ACCELEROMETER

---

## Android在下列哪一個版本後，可以在執行階段要求權限（Request permissions at runtime）？

1. [x] Android 6
2. [ ] Android 7
3. [ ] Android 8
4. [ ] Android 9

---

## 如附圖所示之程式碼，在AndroidManifest.xml檔案中，下列哪一項是正確的屬性定義？

1. [x] 點選LAUNCHER畫面的應用程式圖示可開啟這個Activity
2. [ ] 長按LAUNCHER畫面，彈出的選單中會加入這個Activity選項
3. [ ] Category中可開啟這個Activity
4. [ ] 所有應用程式的選擇畫面中都自動加入這個Activity

---

## 如附圖所示之程式碼，若將附圖之程式碼寫進程式中，會執行下列哪一項動作？

1. [ ] 使用完整路徑呼叫相對應的Intent
2. [ ] 開啟瀏覽器瀏覽網頁
3. [x] 不動作
4. [ ] 開啟Email程式寄送內容資訊

---

## 關於Context#openFileOutput的說明，下列哪一項正確？

1. [x] 參數使用MODE_APPEND，代表在檔案內繼續添加內容而非覆蓋
2. [ ] 參數使用MODE_PRIVATE，代表寫入的檔案連同一個應用程式都無法讀取
3. [ ] 參數使用MODE_READ，代表檔案只能被讀取，不可以重複被寫入
4. [ ] 參數使用MODE_WRITE，檔案才可以被寫入

---

## 在應用程式中，已知一電話號碼myPhone，在程式中要將myPhone直接撥打出去，下列敘述哪一項正確？

1. [x] ^[GD90501]
2. [ ] ^[GD90502]
3. [ ] ^[GD90503]
4. [ ] ^[GD90504]

---

## 下列哪一項方法，可以正確建立LocationManager物件？

1. [x] LocationManager lm =(LocationManager)getSystemService(Context.LOCATION_SERVICE);
2. [ ] LocationManager lm =(LocationManager)getSystemService(Context.LOCATION);
3. [ ] LocationManager lm =(LocationManager)getSystemService(Context.FINE_LOCATION);
4. [ ] LocationManager lm =(LocationManager)getSystemService(Context.LOCATION_PROVIDER);

---

## 使用下列哪一項方式可以取得下載快取目錄？

1. [ ] Environment.getExternalStorageDirectory()
2. [ ] Environment.getDataDirectory()
3. [x] Environment.getDownloadCacheDirectory()
4. [ ] Environment.getRootDirectory()

---

## 在應用程式中我們常使用ConnectivityManager來檢查網路連線狀態，其中ConnectivityManager.getNetworkInfo(int networkType)可以取得網路相關資訊，請問若要檢查行動網路是否有連線，networkType需要傳入下列哪一個參數？

1. [x] ConnectivityManager.TYPE_MOBILE
2. [ ] ConnectivityManager.TYPE_INTERNET
3. [ ] ConnectivityManager.TYPE_4G
4. [ ] ConnectivityManager.TYPE_WIFI

---

## 關於單元測試應具備的特性，下列敘述哪些正確？

1. [x] 快速、獨立
2. [x] 可重複
3. [x] 及時，單元測試應該恰好在使其通過的產品code之前撰寫
4. [x] 可反應驗證結果，單元測試不論成功或失敗，都應該要從測試的報告中直接了解其含意

---

## 關於Activity的敘述，下列哪一項正確？

1. [ ] 一個Application中只能有一個Activity
2. [x] 使用者可以從一個程式中的Activity跳到另一個程式中的Activity
3. [ ] 要切換到另一個Activity，應先按[HOME]鍵回到桌面，再點選應用程式按鈕以進入Activity
4. [ ] Android未提供從目前Activity回到前一個Activity的功能

---

## 下列哪些為SQLite提供的資料型態？

1. [x] INTEGER
2. [x] REAL
3. [x] TEXT
4. [ ] BOOLEAN

---

## 如附圖所示之程式碼，為一字串陣列設定碼，下列哪一項可以正確讀取planets_array字串陣列？

1. [x] String[] planets = getResources().getStringArray(R.array.planets_array);
2. [ ] String planets = getResources().getStringArray(R.array.planets_array);
3. [ ] String[] planets = getResources().getString(R.array.planets_array);
4. [ ] String planets = getResources().getString(R.array.planets_array);

---

## 下列哪一個Android SDK影音類別提供呼叫硬體編解碼功能？

1. [x] MediaCodec
2. [ ] VideoView
3. [ ] MediaPlayer
4. [ ] MediaController

---

## 關於SQLiteOpenHelper的敘述，下列哪些正確？

1. [ ] 應用程式啟動時，每次都會呼叫SQLiteOpenHelper#onCreate方法
2. [ ] 應用程式啟動時，每次都會呼叫SQLiteOpenHelper#onUpgrade方法
3. [x] SQLiteOpenHelper可以建立in-memory database
4. [x] 應用程式啟動時，當資料庫不存在時才會呼叫SQLiteOpenHelper#onCreate方法

---

## 關於AlertDialog.builder基本功能的敘述，下列哪些錯誤？

1. [ ] 其建立對話框的步驟是先定義呼叫點，再實作對話框
2. [ ] 可以在其中加入標題圖示
3. [x] 可以控制其顯示位置
4. [x] 可以控制其顯示時間的長短

---

## 關於SmsManager的敘述，下列哪一項錯誤？

1. [ ] divideMessage()方法可將簡訊內容拆成數則，以手機單則最大字數為限
2. [x] sendSMSMessage()方法可傳送文字型態的簡訊，透過指定的PendingIntent還可知道傳送結果
3. [ ] 傳送簡訊方法中傳入的PendingIntent，可聆聽系統廣播的發送結果
4. [ ] getDefault()方法為取得SmsManager的預設實例（Instance）

---

## 假設gson.jar與httpclient.jar兩個檔案均已放到模組的libs資料夾內；若模組需要使用到這兩個套件，下列哪一項方式，可以手動修改build.gradle的相關性設定（只加入一行參數）？

1. [ ] compile project(':gson.jar')
2. [ ] compile files('libs/gson.jar')
3. [x] compile fileTree(dir:'libs',include:['*.jar'])
4. [ ] compile files('libs/httpclient.jar')

---

## 關於Git系統中常用的指令，下列哪些正確？

1. [x] 初始化一個git repository的指令為git init
2. [x] 檢查git repository目前（commit/push）狀態的指令為git status
3. [x] 新增commit檔案的指令為git add
4. [x] 刪除commit檔案的指令為git rm

---

## searchable.xml在公開給Quick Search Box時，需要做下列哪些設定？

1. [x] android:includeInGlobalSearch需設定為true
2. [ ] android:includeInGlobalSearch需設定為false
3. [x] android:searchSettingsDescription可設可不設
4. [ ] android:includeInGlobalSearch可設可不設

---

## 下列哪一個版本的Android SDK開始內建指紋辨識的API？

1. [ ] Gingerbread
2. [ ] Jelly Bean
3. [ ] Ice Cream Sandwich
4. [x] Marshmallow

---

## 開發Android App時，通常會在組態設定檔內指定支援度最佳的Android 平台版本，下列哪一項正確？

1. [ ] android:compileSdkVersion
2. [x] android:targetSdkVersion
3. [ ] android:maxSdkVersion
4. [ ] android:minSdkVersion

---

## 寫錄音程式時需先有四個預備動作，下列哪一項非其中之一？

1. [ ] setAudioSource
2. [ ] setAudioEncoder
3. [x] setMaxDuration
4. [ ] setOutputFormat

---

## 定義一個版面配置檔案，需要從上而下依序排列介面元件時，一般會使用下列哪一項版面配置元件？

1. [x] LinearLayout
2. [ ] RelativeLayout
3. [ ] TableLayout
4. [ ] FrameLayout

---

## Android中的DownloadManager系統服務，是使用下列哪一項參數廣播給BroadcastReceiver，通知系統檔案下載完畢？

1. [ ] DownloadManager.ACTION_DOWNLOAD_FINISH
2. [ ] DownloadManager.ACTION_DOWNLOAD_OVER
3. [x] DownloadManager.ACTION_DOWNLOAD_COMPLETE
4. [ ] DownloadManager.ACTION_DOWNLOAD_DONE

---

## Instant Run主要可分為下列哪三種類型？

1. [x] Hot Swap
2. [x] Warm Swap
3. [x] Cold Swap
4. [ ] Cool Swap

---

## 如果要讀取存放在Android內部儲存（Internal storage）上的檔案，需要使用下列哪一個方法？

1. [ ] Context.openFileOutput()
2. [ ] Context.openWriteFile()
3. [ ] Context.openReadFile()
4. [x] Context.openFileInput()

---

## 關於版面配置的敘述，下列哪一項錯誤？

1. [ ] 可以透過程式直接定義版面配置
2. [x] 在版面配置檔案中，以<?layout標籤作開頭
3. [ ] 可以使用的Layout包含LinearLayout、RelativeLayout
4. [ ] 可以透過XML定義選單

---

## 關於AlertDialog.builder基本功能的敘述，下列哪一項錯誤？

1. [ ] 可以用來建立對話框
2. [ ] 對話框會重疊顯示到原本的呼叫頁面上
3. [ ] 對話框也是一種顯示內容（View）
4. [x] 可以控制其顯示時間的長短

---

## AnimationDrawable可以製作出如同動態GIF圖檔的動畫效果，下列哪一項屬性的設定可以讓動畫啟動後，只跑一個循環就停止？

1. [x] android:oneshot="true"
2. [ ] android:oneshot="false"
3. [ ] android:run="1"
4. [ ] android:run="0"

---

## 建立LocationListener物件後，下列哪一項方法不一定要被覆寫？

1. [ ] onLocationChanged()
2. [ ] onProviderDisabled()
3. [ ] onStatusChanged()
4. [x] onProviderChanged()

---

## 若要建置程式碼並執行「Run 'app'」，下列指令哪一項正確？

1. [ ] [Shift] + [F8]
2. [ ] [Shift] + [F9]
3. [x] [Shift] + [F10]
4. [ ] [Shift] + [F11]

---

## 應用程式上架後，若後續有新版本上架更新，安裝過該App的使用者會收到通知。請問在Gradle組態設定中，下列哪一個參數「一定」要變更？

1. [ ] applicationId
2. [ ] minSdkVersion
3. [x] versionCode
4. [ ] versionName

---

## 在Activity運作中按下[BACK]鍵，Activity最後會呼叫下列哪一項方法？

1. [x] onDestroy
2. [ ] onStop
3. [ ] onStopCommand
4. [ ] onPause

---

## 如附圖所示之程式碼，關於content URI的敘述，下列哪些錯誤？

1. [x] C部分必須至少有一節
2. [ ] A部分為prefix，不可以變更
3. [x] D部分為指定某一筆資料的_id，如果要取得所有資料時可以將URI改為content://com.example.transportation/provider/trains
4. [ ] B部分應該使用包含package的完整類別名稱，以保持獨特性

---

## Android 6.0中引入了新的權限管理模型，下列敘述哪些正確？

1. [x] 允許應用程式在執行時期（RunTime）提示使用者是否授予權限
2. [x] 被歸類於PROTECTION_NORMAL的權限系統會直接授予，不需特別設定
3. [x] 權限被群組化
4. [ ] 針對所有的權限，如果在程式中需執行相關權限的動作時，必須檢查是否有授予該權限

---

## 在Android.app.Service的類別中，下列哪一項方法是建構一個Service時一定要覆寫的方法？

1. [x] onBind()
2. [ ] onCreate()
3. [ ] onStart()
4. [ ] onStop()

---

## 下列哪一項方法可以調整Paint物件的畫筆寬度？

1. [x] Paint.setStrokeWidth(float width);
2. [ ] Paint.setStrokeMiter(float miter);
3. [ ] Paint.setStrokeCap(Cap cap);
4. [ ] Paint.setStrokeJoin(Join join);

---

## 下列哪一項是監聽手機設備狀態改變的方法？

1. [x] public void onCallStateChanged(int state,String incomingNumber)
2. [ ] public void onCellLocationChanged(CellLocation location)
3. [ ] public void onDataConnectionStateChanged(int state)
4. [ ] public void onDataConnectionStateChanged(int state,int networkType)

---

## 若要在res目錄中定義程式使用的陣列、字串、尺寸、色彩和樣式的常數值，需在下列哪一項進行設定？

1. [ ] layout
2. [x] values
3. [ ] menu
4. [ ] drawable

---

## 如附圖所示之模組參數設定，假設開發中的Android專案沒有任何語法或設定錯誤，當點擊「Run 'app'」進行開發時期的測試，此時系統會自動產生可執行檔案（APK）。請問下列哪一個APK檔案會被生成？

1. [ ] app-freeVersion-release.apk
2. [ ] app-freeVersion-debug.apk
3. [ ] app-freeVersion.apk
4. [x] app-debug.apk

---

## 在Android 9.0中，如果要取得Wi-Fi的相關資料（如：SSIDs、BSSIDs…等），需呼叫下列WifiManager的哪一個方法？

1. [x] getConnectionInfo()
2. [ ] getDhcpInfo()
3. [ ] getConfiguredNetworks()
4. [ ] getScanResults()

---

## Logcat使用的方法（Method）中，下列哪一項是用於將系統錯誤訊息列出的方法？

1. [ ] Log.i(string,string)
2. [x] Log.e(string,string)
3. [ ] Log.w(string,string)
4. [ ] Log.v(string,string)

---

## 關於TabHost版面配置元件的敘述，下列哪一項錯誤？

1. [x] TabHost元件中，不可包含多個Activity
2. [ ] TabHost元件中，可以包含多個RelativeLayout元件
3. [ ] TabHost元件中，可以包含多個TabWidget元件
4. [ ] TabHost元件中，可以包含多個TableLayout元件

---

## 關於EditText元件中，加入不同inputType屬性值效果的敘述，下列哪些正確？

1. [x] 在inputType中加入phone宣告，可以讓欄位只能輸入電話號碼
2. [x] 在inputType中加入date宣告，可以讓欄位只能輸入日期
3. [x] 在inputType中加入textMultiLine宣告，可以允許多行輸入
4. [x] 在inputType中加入textPassword宣告，可以讓欄位以密碼（*號）顯示

---

## 關於EditText元件的敘述，下列哪些正確？

1. [ ] 在EditText元件中加入android:number宣告，可以讓過長的字串以跑馬燈效果顯示
2. [x] EditText類別繼承自TextView類別
3. [ ] EditText類別繼承自UI類別
4. [x] 在EditText元件中加入android:inputType宣告，可以預先指定輸入內容的類型

---

## 如附圖所示之程式碼，使用MediaPlayer播放音樂，下列敘述哪一項錯誤？

1. [ ] 缺少mp.seek(0);的程式描述，需在prepare()之前呼叫seek()方法
2. [x] 呼叫setDataSource與prepare()皆需處理IOException的例外處理
3. [ ] 呼叫prepare()方法需要處理IOException的例外處理
4. [ ] 呼叫start()方法需要處理IOException的例外處理

---

## 關於TableLayout版面配置元件的敘述，下列哪一項正確？

1. [ ] TableLayout元件中，不可包含多層版面
2. [ ] 一個畫面中不可包含多個TableLayout元件
3. [x] TableLayout元件需搭配TableRow使用
4. [ ] 可以在TableLayout的內容元件中，指定android:layout_column來合併欄位

---

## 要實作Android Widget桌面小工具時，必須繼承下列哪一項類別？

1. [ ] Widget
2. [ ] AppWidget
3. [ ] WidgetProvider
4. [x] AppWidgetProvider

---

## 活動（Activity）會因為組態的改變（如：由直立變成橫放），而被摧毀且再重新啟動；通常會藉由Bundle來保存與還原重要的資料。但是，複雜的資料（如：一般自訂類別）就無法直接利用Bundle記錄。下列哪一個內建類別適合用來記錄（取回）複雜的資料內容？

1. [ ] Bundle
2. [x] Fragment
3. [ ] Activity
4. [ ] Message

---

## 在Wear OS製作訊息顯示服務時，下列哪些是可用的通知樣式？

1. [x] BIG_TEXT_STYLE
2. [x] BIG_PICTURE_STYLE
3. [x] INBOX_STYLE
4. [x] MESSAGING_STYLE

---

## Instant Run的主要設計目的，下列哪些正確？

1. [x] 快速建置
2. [x] 部署
3. [ ] 版本控制
4. [ ] 專案管理

---

## 使用下列哪一個語法可以取得外部裝置？

1. [x] Environment.getExternalStorageDirectory()
2. [ ] Environment.getDataDirectory()
3. [ ] Environment.getDownloadCacheDirectory()
4. [ ] Environment.getRootDirectory()

---

## 由於Android TV需要APP使用Leanback的使用者介面，所以必須在AndroidManifest.xml中加入下列哪一項使用特性的聲明？

1. [x] android.software.leanback
2. [ ] android.software.vr.mode
3. [ ] android.software.live_wallpaper
4. [ ] android.software.app_widgets

---

## 要製作一個提供檔案的ContentProvider，不需實作下列哪些方法？

1. [ ] onCreate
2. [ ] getType
3. [x] UPDATE
4. [x] openFile

---

## 如要啟動自動填表（Autofill Framework）服務，需要在手機設定中的哪裡開啟？

1. [x] Settings > System > Languages & input > Advanced > Input assistance > Autofill service.
2. [ ] Settings > System > Advanced > Languages & input > Input assistance > Autofill service.
3. [ ] Settings > Languages & input > System > Advanced > Input assistance > Autofill service.
4. [ ] Settings > Input assistance >System > Languages & input > Advanced > Autofill service.

---

## 在應用程式中要透過JNI來與C++程式溝通時，要使用下列哪一行程式來指定要呼叫的C++程式？

1. [x] System.loadLibrary("XXX")
2. [ ] JNI.loadLibrary("XXX")
3. [ ] System.load ("XXX")
4. [ ] JNI.load("XXX")

---

## 若要利用Android Support Library進行程式開發，需利用Android SDK Manager進行下載、安裝。在開啟SDK Manager和點選Extras之後，下列哪些項目一定要勾選並下載？

1. [x] Android Support Repository
2. [ ] Google Repository
3. [x] Android Support Library
4. [ ] Google Play Service

---

## 在不同的Activity間傳遞資料是透過Bundle物件來處理，但若要讓資料能夠在兩個Activity間往返自如，則必須要使用下列哪一項方法？

1. [ ] startActivity()
2. [ ] bindService()
3. [ ] sendBroadcast()
4. [x] startActivityForResult()

---

## 要在應用程式中設計一個EditText，並只允許使用者輸入電話號碼格式，可以在Layout中的EditText宣告裡加入下列哪一項屬性設定？

1. [ ] android:inputType="number"
2. [x] android:inputType="phone"
3. [ ] android:inputType="tel"
4. [ ] android:inputType="text"

---

## 關於android.content.URIMatcher的敘述，下列哪些正確？

1. [x] 用來幫助Content Provider內設定和辨識URI所要存取的資源使用
2. [x] addURI(String authority, String path, int code)方法的第二個變數path中可以使用#代表數字
3. [x] addURI(String authority, String path, int code)方法的第二個變數path中可以使用*代表任意字串
4. [ ] addURI(String authority, String path, int code)方法的第三個變數code可以為負數

---

## 開發Android App時，通常需要指定應用程式的個性化圖示與標題文字，請問下列敘述哪些正確？

1. [x] 利用<application>的屬性android:icon指定應用程式圖示
2. [ ] 利用<application>的屬性android:picture指定應用程式圖示
3. [x] 利用<application>的屬性android:label指定應用程式的標題文字
4. [ ] 利用<application>的屬性android:title指定應用程式的標題文字

---

## 關於查詢Content Provider所提供的資料，下列敘述哪些正確？

1. [x] 需使用URI來辨別Provider
2. [ ] 不需提供要取得資料的欄位名稱
3. [ ] 不需知道資料型態就可以存取
4. [x] URI為content://為開頭

---

## BluetoothDevice物件提供了一些方法來取得藍牙裝置的相關資訊，下列哪些方法正確？

1. [x] getName()
2. [x] getAddress()
3. [ ] getBondStatus()
4. [ ] getUid()

---

## 關於AlertDialog的敘述，下列哪一項錯誤？

1. [ ] 使用show()顯示AlertDialog視窗
2. [ ] AlertDialog元件是由Title、Message所組成，實際應用情況是將特定訊息發送給使用者，並按下確認按鈕使AlertDialog消失
3. [x] 可使用setChoiceItems方法加入多選按鈕
4. [ ] 可使用setMessage方法加入Dialog內容敘述

---

## 關於SharedPreferences的敘述，下列哪些正確？

1. [x] 可以透過Context.getSharedPreferences("name",0)取得
2. [x] 有getBoolean(String key, boolean defValue), getString(String key, String defValue)...等方法
3. [x] 如果在非PreferenceActivity中變更資料，需呼叫SharedPreferences.Editor.commit()方法
4. [ ] 寫入的內容在其他應用程式也一定能存取

---

## Android平台上一般的應用程式屬於Android框架的哪一層？

1. [x] Application層
2. [ ] Application Framework層
3. [ ] Libraries層
4. [ ] Kernel & Driver層

---

## 下列哪一項不是預設的Preference元件？

1. [ ] CheckBoxPreference
2. [ ] DialogPreference
3. [ ] EditTextPreference
4. [x] TextareaPreference

---

## 下列哪一項是呼叫startActivityForResult(Intent,int)方法時，後面的int類別的作用？

1. [ ] 用作Intent代號
2. [ ] 用作Resource代號
3. [x] 用作識別的代號
4. [ ] 用作Activity的代號

---

## 關於版面配置的敘述，下列哪一項錯誤？

1. [ ] 一個畫面中可以包含多層版面配置
2. [ ] 一個畫面中可以包含多個版面配置
3. [x] 可以使用的Layout包含FrameLayout、HtmlLayout
4. [ ] 在LinearLayout元件中，可以透過android:orientation屬性定義內容元件排列方向

---

## 下列哪一項是建立RecyclerView元件所使用的程式語法？

1. [ ] android.v7.widget.RecyclerView
2. [ ] android.support.widget.RecyclerView
3. [x] android.support.v7.widget.RecyclerView
4. [ ] android.control.v7.widget.RecyclerView

---

## 當使用者點選mailto:info@example.com連結時，Android會用到下列哪一項物件？

1. [x] Intent
2. [ ] Service
3. [ ] ContentProvider
4. [ ] EmailManager

---

## 若要在Android Studio中修改SDK路徑或JDK路徑的設定，可以在下列哪一項進行設定？

1. [ ] File/Settings
2. [x] File/Project Structure
3. [ ] File/Import Settings
4. [ ] File/Export Settings

---

## Android中的DownloadManager系統服務，下列哪一項類別可用於過濾DownloadManager的查詢？

1. [ ] DownloadManager.Start
2. [ ] DownloadManager.Request
3. [x] DownloadManager.Query
4. [ ] DownloadManager.Open

---

## 下列哪一項是Android Stutio預設使用的自動化建置工具？

1. [ ] Ant
2. [x] Gradle
3. [ ] Maven
4. [ ] Makefile

---

## 想在電視設備上執行Android應用程式，必須在AndroidManifest.xml中，替Activity增加下列哪一項過濾器中的類別才可以讓Google Play區分識別？

1. [ ] android.intent.category.LAUNCHER
2. [x] android.intent.category.LEANBACK_LAUNCHER
3. [ ] android.intent.category.DEFAULT
4. [ ] android.intent.category.EMBED

---

## 如果要在type-to-search時在自己的應用程式內做搜尋，下列哪一項正確？

1. [x] 在Activity內呼叫setDefaultKeyMode(DEFAULT_KEYS_SEARCH_LOCAL)
2. [ ] 無法修改此預設行為
3. [ ] 在Activity內呼叫setDefaultKeyMode(NONE_GLOBAL_SEARCH)
4. [ ] 在Activity內呼叫setDefaultKeyMode(DEFAULT_KEYS_SEARCH_GLOBAL)

---

## 在應用程式中可使用PowerManager.WakeLock來控制手持設備螢幕維持喚醒狀態，下列哪一個方法在離開程式時需被呼叫以釋放被喚醒狀態？

1. [ ] destroy()
2. [ ] acquire()
3. [x] release()
4. [ ] disconnect()

---

## 如附圖所示之專案參數設定，下列哪一項是編譯後action屬性name的內容值？

1. [ ] ${applicationId}.VIEW
2. [ ] tw.com.tqc.demo1
3. [x] tw.com.tqc.demo1.VIEW
4. [ ] VIEW

---

## 關於Context#openFileInput的使用方式，下列哪一項可以正確執行（假設路徑上檔案都存在）？

1. [x] openFileInput("aaa")
2. [ ] openFileInput("../aaa")
3. [ ] openFileInput("aaa/bbb")
4. [ ] openFileInput("../files/aaa")

---

## 如果要將資料存放到外部儲存（External Storage），需要開啟下列哪一項權限？

1. [ ] android.permission.INTERNET
2. [x] android.permission.WRITE_EXTERNAL_STORAGE
3. [ ] android.permission.WRITE_SETTINGS
4. [ ] android.permission.READ_EXTERNAL_STORAGE

---

## Android提供Multi-window多視窗顯示，可以同時顯示多個應用程式的功能，下列哪一項配置參數是開啟或停用多視窗顯示的屬性？

1. [x] android:resizeableActivity
2. [ ] android:supportsPictureInPicture
3. [ ] android:inMultiWindowAndroid
4. [ ] android:inPictureInPicture

---

## 關於Service的敘述，下列哪一項正確？

1. [ ] Service負責描繪使用者介面
2. [ ] 長時間運行Service會造成程式執行延遲
3. [x] 不會造成目前執行的Activity停止
4. [ ] 即使系統記憶體不足時，Service也不會無預警被關閉

---

## 利用Android Studio開發專案時，若要匯入專案的設定資訊，需要利用下列哪一項功能？

1. [ ] File/Settings
2. [ ] File/Project Structure
3. [x] File/Import Settings
4. [ ] File/Export Settings

---

## 下列哪一項為Log語句的正確格式？

1. [x] Log.d(識別字串,記錄內容)
2. [ ] Log.e(記錄內容,代號)
3. [ ] Log.s(識別字串,記錄內容)
4. [ ] Log.d(記錄內容,代號)

---

## 要在程式中取得SIM卡內的相關資訊，可使用下列哪一項Manager物件？

1. [x] android.telephony.TelephonyManager
2. [ ] android.telephony.SmsManager
3. [ ] android.telephony.SimManager
4. [ ] android.telephony.ContentManager

---

## 通知通道（Notification Channel）需要設定的資料包含下列哪些？

1. [x] channel id
2. [x] channel name
3. [x] channel priority
4. [ ] channel icon

---

## 在應用程式中要搜尋附近的藍牙裝置時，需註冊一個BroadcastReceiver，並攔截下列哪一個廣播訊息事件？

1. [x] BluetoothDevice.ACTION_FOUND
2. [ ] BluetoothDevice.BOND_BONDED
3. [ ] BluetoothDevice.ACTION_ACL_CONNECTED
4. [ ] BluetoothDevice.ACTION_DEVICE_FOUND

---

## 下列哪一項不是手機接聽電話的狀態？

1. [ ] TelephonyManager.CALL_STATE_IDLE
2. [ ] TelephonyManager.CALL_STATE_OFFHOOK
3. [ ] TelephonyManager.CALL_STATE_RINGING
4. [x] TelephonyManager.CALL_STATE_HOOK

---

## 關於Button介面元件的敘述，下列哪一項錯誤？

1. [ ] Button類別繼承自TextView類別
2. [ ] 可以透過Button.setOnClickListener函式來為按鈕加上事件處理函式
3. [ ] 可以透過在XML檔定義Button時加入android:onClick屬性來為按鈕加上事件處理函式
4. [x] 可以在程式中透過Button.setPositiveButton語句來建立獨立的按鈕

---

## 關於影音類別的敘述，下列哪一項正確？

1. [ ] 可以用MediaRecoder類別來播放影音
2. [ ] 可以用MediaController類別來控制語音輸入
3. [ ] 可以用MediaController控制圖片輪播
4. [x] 可以用MediaPlayer類別來播放影音

---

## Android提供下列哪一項Activity供開發者可以繼承，並迅速開發應用程式的設定畫面？

1. [x] PreferenceActivity
2. [ ] MapActivity
3. [ ] SettingActivity
4. [ ] TabActivity

---

## 在Android Studio中有許多檢視專案結構的瀏覽模式，包含下列哪些項目？

1. [ ] Scratches
2. [ ] Problems
3. [x] Project
4. [x] Packages

---

## 開發計步器應用程式時，會使用到下列哪些Sensor？

1. [x] Sensor.TYPE_STEP_COUNTER
2. [ ] Sensor.TYPE_PRESSURE
3. [x] Sensor.TYPE_STEP_DETECTOR
4. [ ] Sensor.TYPE_ACCELEROMETER

---

## Android系統未內建支援下列哪一項檔案格式？

1. [ ] .mp3
2. [ ] .jpg
3. [x] .rm
4. [ ] .png

---

## 在Android中，如果要使用拒接來電（BlockedNumberContract）功能，需在何處新增拒接號碼？

1. [ ] ContactsContract.Contacts.CONTENT_URI
2. [ ] ContactsContract.CommonDataKinds.Phone.CONTENT_URI
3. [x] BlockedNumberContract.BlockedNumbers.CONTENT_URI
4. [ ] ContactsContract.Contacts.CONTENT_FILTER_URI

---

## 如附圖所示之JSON格式資料，如果將其命名為strJSONValue，下列哪一段程式碼可以解析出「大雄」？

1. [x] ^[GD90401]
2. [ ] ^[GD90402]
3. [ ] ^[GD90403]
4. [ ] ^[GD90404]

---

## 關於android.content.ContentProvider.getType(URI uri)的敘述，下列哪些正確？

1. [x] 回傳傳入URI對應資料的MIME Type
2. [x] 如果對應的資料為單筆，則應回傳vnd.android.cursor.item/vnd.yourcompanyname.contenttype
3. [x] 如果對應的資料為數筆，則應回傳vnd.android.cursor.dir/vnd.yourcompanyname.contenttype
4. [ ] 此方法為ContentProvider的Final Method，不應該被自訂的Content Provider覆寫

---

## 繼承Content Provider時，下列哪些方法必須自行實作？

1. [x] QUERY
2. [x] INSERT
3. [x] DELETE
4. [ ] DROP

---

## 關於Broadcast Receiver的敘述，下列哪一項錯誤？

1. [ ] 在Broadcast Receiver中可透過NotificationManager產生提示訊息
2. [ ] Broadcast Receiver主要運行在onReceive函式中
3. [ ] 可以在Broadcast Receiver中start Service
4. [x] Broadcast Intent後會啟動對應的Activity

---

## Android模擬器可以模擬下列哪些內容？

1. [x] GPS位置
2. [x] 來電顯示
3. [x] 簡訊
4. [x] 通話（撥打電話、接聽電話）

---

## 當使用者點選http://www.example.com連結時，Android會用到下列哪一項物件？

1. [x] Intent
2. [ ] HttpManager
3. [ ] Chrome
4. [ ] WebManager

---

## 關於在Android上播放影音，下列哪一項錯誤？

1. [ ] 支援串流播放
2. [ ] 支援播放在記憶卡中的影像
3. [x] 系統內建的播放器可列出應用程式中的影像
4. [ ] 其他應用程式可直接呼叫系統內建的播放器

---

## 在Activity中要保存目前程式的狀態，適合於運行到下列哪一項方法時儲存？

1. [ ] onDestroy
2. [ ] onStop
3. [ ] onResume
4. [x] onPause

---

## 若要下載Android版本升級套件或API升級套件，需在下列哪一項進行設定？

1. [ ] Navigation Editor
2. [ ] Android Device Monitor
3. [ ] AVD Manager
4. [x] SDK Manager

---

## 下列哪一項為錯誤的Log方法？

1. [ ] Log.d
2. [ ] Log.e
3. [ ] Log.i
4. [x] Log.s

---

## Android中ANR是由下列哪些系統服務監視？

1. [x] ActivityManager
2. [x] WindowManager
3. [ ] AccountManager
4. [ ] AccessibilityManager

---

## 如果已經在Android專案中，res/values/styles.xml檔案的<style>標籤中定義完成一個名稱（name）為mystyle的樣式，下列哪一個屬性設定可以將mystyle樣式套用到TextView元件上？

1. [ ] theme = "@style/mystyle"
2. [x] style = "@style/mystyle"
3. [ ] name = "@style/mystyle"
4. [ ] label = "@style/mystyle"

---

## 下列哪些intent-filter內的設定，是在實現APP Links功能時所必須設定的？

1. [x] <action android:name="android.intent.action.VIEW"/>
2. [ ] <action android:name="android.intent.action.GET_CONTENT"/>
3. [x] <category android:name="android.intent.category.BROWSABLE"/>
4. [ ] <category android:name="android.intent.category.LINKS"/>

---

## Android開發工具所支援的作業系統有下列哪些？

1. [x] Windows
2. [x] Linux
3. [x] Mac OS X
4. [ ] FreeBSD

---

## 下列哪一項屬於System Service？

1. [x] NotificationManager
2. [ ] ContentProvider
3. [ ] MediaService
4. [ ] PreferenceActivity

---

## 如果要使用JobScheduler來執行背景工作，Android的API版本需要在幾版以上才有支援？

1. [x] API 21（Android 5.0）
2. [ ] API 23（Android 6.0）
3. [ ] API 25（Android 7.1）
4. [ ] API 27（Android 8.1）

---

## 關於LinearLayout版面配置元件的敘述，下列哪一項錯誤？

1. [ ] LinearLayout元件中，可以包含多層版面
2. [ ] 一個畫面中可以包含多個LinearLayout元件
3. [x] 在一個LinearLayout元件中，可以同時定義直向與橫向顯示內容元件
4. [ ] 在LinearLayout元件中，可以透過android:orientation屬性定義內容元件排列方向

---

## 下列哪些PowerManager屬性值，可預防螢幕進入睡眠模式？

1. [x] FULL_WAKE_LOCK
2. [x] SCREEN_BRIGHT_WAKE_LOCK
3. [x] SCREEN_DIM_WAKE_LOCK
4. [ ] PROXIMITY_SCREEN_OFF_WAKE_LOCK

---

## 下列哪些指令具有搜尋程式碼的功能？

1. [x] [Ctrl] + [F]
2. [ ] [Ctrl] + [Alt] + [O]
3. [ ] [Ctrl] + [Shift] + [Enter]
4. [x] [Ctrl] + [Shift] + [F]

---

## 關於AlertDialog的敘述，下列哪一項錯誤？

1. [ ] 可使用Builder方法建立Dialog內容
2. [ ] 可使用setTitle方法加入Dialog標題
3. [x] 可使用setButton方法加入Dialog按鈕
4. [ ] 可使用show方法顯示Dialog對話視窗

---

## Android預設會將資料庫儲存的資料存放在下列哪一項目錄下？

1. [x] /data/data/package_name/databases
2. [ ] /data/app/package_name
3. [ ] /data/app/package_name/databases
4. [ ] /data/app

---

## 要處理Broadcast Receiver，應該實作下列哪一項函式？

1. [ ] onStart
2. [x] onReceive
3. [ ] onBroadcast
4. [ ] onCatch

---

## 在Android Things APP中，要控制週邊IO設備（例如：GPIO、PWM、I2C、SPI等等），需加入下列哪一項權限？

1. [x] com.google.android.things.permission.USE_PERIPHERAL_IO
2. [ ] com.google.android.things.permission.MANAGE_SENSOR_DRIVERS
3. [ ] com.google.android.things.permission.MANAGE_INPUT_DRIVERS
4. [ ] com.google.android.things.permission.MANAGE_GNSS_DRIVERS

---

## 如果要畫一個三角形，需要用到下列哪一項android.graphics的class？

1. [ ] Rect
2. [x] Path
3. [ ] RectF
4. [ ] Triangle

---

## Google在Android哪一個版本時引進了新的簽名版本V2（Signature Version V2），提供更高的安全性，不能對簽名後的APK做任何修改？

1. [ ] Android 6
2. [x] Android 7
3. [ ] Android 8
4. [ ] Android 9

---

## 使用WifiManager來掃描附近Wi-Fi熱點時，需註冊一個BroadcastReceiver，並攔截下列哪一個廣播訊息事件？

1. [ ] WifiManager.RSSI_CHANGED_ACTION
2. [ ] WifiManager.EXTRA_NETWORK_INFO
3. [x] WifiManager.SCAN_RESULTS_AVAILABLE_ACTION
4. [ ] WifiManager.SCAN_RESULTS_ACTION

---

## 利用數位簽章（金鑰）進行簽章過的APK檔案（Singed APK）方能上傳到Google Play。當我們設定數位簽章參數時需要輸入兩種密碼：金鑰檔案庫密碼以及金鑰密碼；而這些參數在設定後會儲存在模組內的組態設定檔（build.gradle）。請問下列哪一個參數代表「金鑰密碼」？

1. [ ] keyAlias
2. [x] keyPassword
3. [ ] storeFile
4. [ ] storePassword

---

## AndroidManifest.xml是Android應用程式中最重要的文件之一，記錄了應用程式的許多資料，下列哪些項目包含在內？

1. [x] 應用程式的完整名稱
2. [x] 應用程式包含的活動、內容提供者、廣播接收器和服務元件
3. [x] 宣告應用程式執行時需要的權限，例如：存取網路和GPS等
4. [x] 應用程式的目標和最小需求的API層級

---

## 系統內建的Home（桌面）程式屬於Android框架的哪一層？

1. [x] Application層
2. [ ] Application Framework層
3. [ ] Libraries層
4. [ ] Kernel & Driver層

---

## 下列哪一項不是Android訊息通知（Notification）一定要設定的資料？

1. [ ] setSmallIcon
2. [ ] setContentTitle
3. [x] setContentIntent
4. [ ] setContentText

---

## 權限管理將權限分為兩大類：一般權限與危險權限，請問下列哪些是屬於危險權限？

1. [x] CALENDAR
2. [x] CAMERA
3. [x] LOCATION
4. [x] MICROPHONE

---

## 下列哪一項內建介面元件，設計目的是為了讓使用者在輸入內容時，能得到提示？

1. [x] AutoCompleteTextView
2. [ ] EditText
3. [ ] SeekBar
4. [ ] RatingBar

---

## Android預設會將Preference的檔案存放在下列哪一項目錄下？

1. [ ] /data/data/package_name/files
2. [ ] /data/app/package_name/files
3. [x] /data/data/package_name/shared_pref
4. [ ] /data/app/package_name/databases

---

## 在Android Studio新建專案的過程中，需勾選開發的應用程式種類，下列哪些選項包含在此範圍內？

1. [x] Phone and Tablet
2. [x] TV
3. [x] Wear
4. [x] Glass

---

## 當使用者在PreferenceActivity內變更設定時，若要即時反應到App中，下列哪一項正確？

1. [x] 實作SharedPreferences.OnSharedPreferenceChangeListener的onSharedPreferenceChanged並註冊，當設定變更時會觸發
2. [ ] 實作Activity的onSharedPreferenceChanged
3. [ ] 需重新開啟App才有辦法反應變更結果
4. [ ] 實作Activity的onChangeListener

---

## 要寫一個呼叫不同程式的Activity，下列哪一項設定正確？

1. [x] 使用Intent.setClassName()方法呼叫Intent
2. [ ] 使用ContentProvider呼叫Intent
3. [ ] 使service定義背景呼叫Intent
4. [ ] 不同應用程式間不可直接呼叫Activity

---

## Android中的DownloadManager系統服務，是使用下列哪一個方法搭配DownloadManager.Request來取得下載檔案的編號（id）？

1. [ ] query()
2. [x] enqueue()
3. [ ] start()
4. [ ] begin()

---

## 關於Notification訊息的敘述，下列哪一項正確？

1. [x] 可以呼叫notify方法來顯示Notification訊息
2. [ ] 可以呼叫cancel方法來顯示Notification訊息
3. [ ] 可以呼叫cancelAll方法來顯示Notification訊息
4. [ ] 可以呼叫show方法來顯示Notification訊息

---

## 開發Android App時，通常會在組態設定檔內指定Android SDK編譯環境的版本，下列哪一項正確？

1. [x] android:compileSdkVersion
2. [ ] android:targetSdkVersion
3. [ ] android:maxSdkVersion
4. [ ] android:minSdkVersion

---

## 如附圖所示之程式碼，下列哪一項是DAY_VIEW_MODE參數的正確型別？

1. [x] int
2. [ ] string
3. [ ] double
4. [ ] list

---

## 關於Android Studio匯入專案的敘述，下列哪些正確？

1. [x] 匯入後某些檔案的位置可能不同
2. [ ] 程式碼需要重新撰寫
3. [x] API版本不同時需要重新調整
4. [x] SDK的路徑可能不一致，需要調整為本機電腦的SDK路徑

---

## 若要使用快捷鍵Optimize Imports的功能自動整理Imports，下列指令哪一項正確？

1. [ ] [Ctrl] + [Shift] + [I]
2. [x] [Ctrl] + [Alt] + [O]
3. [ ] [Ctrl] + [Shift] + [F]
4. [ ] [Ctrl] + [Shift] + [Enter]

---

## 下列哪些內建介面元件，設計目的為讓使用者只能選擇其中一個選項？

1. [x] RadioButton
2. [ ] CheckBox
3. [x] Spinner
4. [ ] ListBox

---

## 關於Preference的敘述，下列哪一項錯誤？

1. [x] 必須繼承PreferenceActivity才能存取Preference
2. [ ] 同一個應用程式都可以存取到同個Preference
3. [ ] 透過registerOnSharedPreferenceChangeListener，當設定變動時會呼叫登錄的callback
4. [ ] PreferenceManager#getDefaultSharedPreferences需傳入android.content.Context

---

## 在Android Studio中，下列哪一項屬於檢查代碼的功能？

1. [ ] Reformat Code
2. [ ] Rearrange Code
3. [x] Inspect Code
4. [ ] Code cleanup

---

## Android的動畫（Animation）是由四種型態（Type）所組成，下列哪一項不屬於Animation的型態？

1. [ ] scale
2. [ ] alpha
3. [ ] rotate
4. [x] delta

---

## 若要使用快捷鍵Reformat Code的功能，下列指令哪一項正確？

1. [ ] [Ctrl] + [F]
2. [ ] [Ctrl] + [Alt] + [O]
3. [ ] [Ctrl] + [Shift] + [Enter]
4. [x] [Ctrl] + [Alt] + [L]

---

## 請問支援程式庫（Android Support Library）的目的，下列敘述哪一項正確？

1. [x] 提供舊版作業系統的手機可以執行新的功能
2. [ ] 提供新版作業系統的手機可以執行舊的功能
3. [ ] 支援未來新手機的硬體規格
4. [ ] 支援未來新手機的軟體功能

---

## 在全名為tw.test.Sample的Activity中使用getSharedPreferences("test",0)時，Preferences的檔名下列哪一項正確？

1. [ ] tw.test_preferences.xml
2. [ ] Sample_preferences.xml
3. [ ] Sample.xml
4. [x] test.xml

---

## 下列哪一項是取得電話服務所需要傳入的名稱？

1. [ ] getSystemService(Context.WIFI_SERVICE)
2. [ ] getSystemService(Context.POWER_SERVICE)
3. [x] getSystemService(Context.TELEPHONY_SERVICE)
4. [ ] getSystemService(Context.SENSOR_SERVICE)

---

## 若要將RecyclerView元件設定成為類似GridView元件且可以水平滑動的Layout，需要呼叫RecyclerView的哪一個LayoutManager？

1. [ ] GridLayoutManager
2. [x] StaggeredGridLayoutManager
3. [ ] LayoutManager
4. [ ] HorizontalGridLayoutManager

---

## 若要新建一個專案，下列哪一項正確？

1. [x] File/New/New Project
2. [ ] File/New/New Module
3. [ ] File/New/Import Project
4. [ ] File/New/Import Module

---

## 關於TextView元件的敘述，下列哪一項正確？

1. [x] EditText類別繼承自TextView類別
2. [ ] 在TextView元件中加入android:color宣告，可以指定文字顏色
3. [ ] TextView元件的作用是提供按鈕功能顯示
4. [ ] 可以在TextView元件中輸入文字，並設定只能輸入數字

---

## 如附圖所示之模組參數設定，假設開發中的Android專案沒有任何語法或設定錯誤，相關金鑰也已完成設定；當點擊「Run 'app'」，系統會自動產生可執行檔案（APK）。請問下列哪一個APK檔案會被生成？

1. [x] app-freeVersion-release.apk
2. [ ] app-freeVersion-debug.apk
3. [ ] app-freeVersion.apk
4. [ ] app-debug.apk

---

## 關於Notification訊息的敘述，下列哪一項錯誤？

1. [ ] 可以在程式中控制Notification訊息的顯示
2. [ ] 可以在程式中取消Notification訊息的顯示
3. [ ] 背景服務可以透過Notification訊息提示運作狀態
4. [x] Notification屬於Content Provider的一種

---

## Android 7引入了網路安全性設定功能，讓應用程式在安全的宣告式設定檔中即可自訂網路安全性設定，而不必修改應用程式的程式碼，此功能的主要能力下列哪些正確？

1. [x] Custom trust anchors自訂信任的憑證管理機構
2. [x] Debug-only overrides僅偵錯覆寫
3. [x] Cleartext traffic opt-out退出明碼流量
4. [x] Certificate pinning憑證關聯

---

## 要取得從另一個Activity返回的結果，應在原Activity中加入下列哪一項方法？

1. [x] onActivityResult
2. [ ] getActivityByResult
3. [ ] getActivityForResult
4. [ ] onReceive

---

## Room產生資料庫的方法下列哪些正確？

1. [x] Room.databaseBuilder
2. [x] Room.inMemoryDatabaseBuilder
3. [ ] Room.MyDBHelper
4. [ ] Room.MemoryDatabaseBuilder

---

## 當需要使用JobScheduler來執行實際的工作時，需要新建一個Service來繼承JobService，並且必須覆寫下列哪些方法？

1. [x] onStartJob(JobParameters params)
2. [ ] onStartCommand(Intent intent, int flags, int startId)
3. [ ] jobFinished(JobParameters params, boolean wantsReschedule)
4. [x] onStopJob(JobParameters params)

---

## 欲開發一應用程式，透過JNI來與C/C++程式溝通，此時C/C++程式要放在專案的下列哪一個資料夾下？

1. [ ] src
2. [x] jni
3. [ ] assets
4. [ ] res

---

## 如附圖所示之程式碼，關於content URI的敘述，下列哪些正確？

1. [ ] B部分為prefix，可以變更
2. [x] A部分為prefix，不可以變更
3. [x] D部分為指定某一筆資料的_id，如果要取得所有資料時可以將URI改為content://com.example.transportationprovider/trains
4. [ ] B部分不應該使用包含package的完整類別名稱，以保持獨特性，另外不可以與<provider>定義authorities的內容一致

---

## 下列哪一項方法，可從一個Activity跳轉到另一個Activity（不需返回資訊）？

1. [x] startActivity
2. [ ] startActivityByResult
3. [ ] startActivityForResult
4. [ ] newActivity

---

## 開發Android App時，若考量「向前相容」的議題，通常會在組態設定檔內指定SDK的必要參數，下列哪一項正確？

1. [ ] android:compileSdkVersion
2. [ ] android:targetSdkVersion
3. [ ] android:maxSdkVersion
4. [x] android:minSdkVersion

---

## 關於Broadcast Receiver的敘述，下列哪一項正確？

1. [ ] 可以在Broadcast Receiver中產生對話框
2. [ ] Broadcast Intent後會啟動對應的Activity
3. [ ] 可以在Broadcast Receiver中bind Service
4. [x] 可透過Context.unregisterReceiver在Activity.onPause()中註銷註冊

---

## 在應用程式中，如欲接收FCM（FireBase Cloud Message）所發出的訊息，需要程式中註冊一個Receiver，該Receiver中需要有一個intent-filter，請問intent-filter的名稱為下列哪一個？

1. [ ] com.google.firebase.messaging.default_notification_icon
2. [x] com.google.firebase.MESSAGING_EVENT
3. [ ] com.google.android.gcm.service.RECEIVE
4. [ ] com.google.android.c2dm.service.RECEIVE

---

## 要從一個Activity呼叫Service啟動時，會用到下列哪一項物件？

1. [ ] ServiceCall
2. [x] Intent
3. [ ] Background
4. [ ] ServiceManager

---

## Android在背景最佳化（Background optimizations）技術中，應用程式現在已經無法發送或接收下列哪些廣播訊息？

1. [x] ACTION_NEW_PICTURE
2. [ ] ACTION_NEW_OUTGOING_CALL
3. [x] ACTION_NEW_VIDEO
4. [ ] 所有廣播訊息皆可接收

---

## 若要套用Android專案中，res/values/styles.xml檔案內所定義的App樣式，需要在下列AndroidManifest.xml檔案中，設定<application>標籤的哪一個屬性？

1. [ ] android:label
2. [ ] android:title
3. [x] android:theme
4. [ ] android:name

---

## 在targetSdkVersion為17或之後版本，必須在與JavaScript互動方法的前方增加下列哪一項標註（Annotation）？

1. [ ] @NavigationMode
2. [x] @JavaScriptInterface
3. [ ] @AnyThread
4. [ ] @RequiresPermission

---

## 下列哪些方法可以在Dialog中加入按鈕？

1. [x] setPositiveButton
2. [x] setNegativeButton
3. [ ] setRightButton
4. [ ] setNormalButton

---

## 關於TextView元件的敘述，下列哪些正確？

1. [x] 在TextView元件中加入android:ellipsize宣告，可以讓過長的字串以跑馬燈效果顯示
2. [ ] 在TextView元件中加入android:color宣告，可以指定文字顏色
3. [x] 在TextView元件中加入android:capitalize宣告，可以指定開頭文字大寫
4. [ ] 在TextView元件中加入android:autoText宣告，可以讓Email自動以連結顯示

---

## 關於Android Studio中GitHub與Git的環境設定，下列哪一項正確？

1. [ ] 使用前不需要先安裝Git
2. [x] 需在「Path to Git executable」設定git.exe的位置
3. [ ] 不需設定GitHub的帳號密碼
4. [ ] 修改程式碼後會自動即時上傳

---

## 開發中文軟體欲使用外部字型檔，須將外部字型檔放置於下列哪一項檔案目錄底下？

1. [x] assets/fonts
2. [ ] res/drawable/fonts
3. [ ] res/layout/fonts
4. [ ] res/value/fonts

---

## 當使用Google assistant時，需覆寫下面哪一個方法來建立ACTION_ASSIST intent以傳遞資料？

1. [x] onProvideAssistData()
2. [ ] onProvideAssistContent()
3. [ ] onProvideStructure()
4. [ ] onProvideVirtualStructure()

---

## 在Android中使用DOM來解析XML文件內容時，需要下列哪些物件才能取得XML文件內所有DOM元素（DOM element）？

1. [x] DocumentBuilderFactory
2. [ ] Element
3. [x] Document
4. [x] DocumentBuilder

---

## 在Git系統中，下列敘述哪些正確？

1. [x] Commit是提交修改記錄到本地端數據庫
2. [ ] Commit是上傳修改記錄遠端數據庫
3. [ ] Push是提交修改記錄本地端數據庫
4. [x] Push是上傳修改記錄遠端數據庫

---

## 下列哪一項不是SQLiteDatabase所提供的方法？

1. [ ] execSQL
2. [ ] delete
3. [ ] insert
4. [x] dropDatabase

---

## 在全名為tw.test.Sample的Activity中使用PreferenceManager.getDefaultSharedPreferences(context)時，Preferences的檔名為下列哪一項？

1. [x] tw.test_preferences.xml
2. [ ] Sample_preferences.xml
3. [ ] Sample.xml
4. [ ] test.xml

---

## 從一個Activity跳轉回到原呼叫的Activity時，經常傳入的RESULT_OK, RESULT_CANCELED參數是屬於下列哪一項型別？

1. [x] int
2. [ ] string
3. [ ] double
4. [ ] list

---

## 定義一個複雜的列表，需要有效率地顯示多個介面元件時，一般會使用下列哪一項版面配置元件？

1. [ ] LinearLayout
2. [x] RelativeLayout
3. [ ] TableLayout
4. [ ] FrameLayout

---

## 若要新建模擬器或是修改模擬器的設定，需在下列哪一項進行設定？

1. [ ] Navigation Editor
2. [ ] Android Device Monitor
3. [x] AVD Manager
4. [ ] SDK Manager

---

## 在Android Studio中，下列哪些項目屬於快速整理程式碼的功能？

1. [x] Reformat Code
2. [ ] Inspect Code
3. [x] Rearrange Code
4. [ ] Code cleanup

---

## 相容性程式庫v7 appcompat library讓程式可以往後支援 Android 2.1 （API level 7）以使用一些新功能。請問下列哪些元件是v7 appcompat library所支援的新元件？

1. [x] RecyclerView
2. [x] GridLayout
3. [x] CardView
4. [ ] TableLayout

---

## 如果要確認Android應用程式是否在電視設備上運行，可以使用下列哪一項方法檢查App是否在電視模式下運行？

1. [ ] UiModeManager.getCurrentModeType() == Configuration.UI_MODE_TYPE_WATCH
2. [x] UiModeManager.getCurrentModeType() == Configuration.UI_MODE_TYPE_TELEVISION
3. [ ] UiModeManager.getCurrentModeType() == Configuration.UI_MODE_TYPE_CAR
4. [ ] UiModeManager.getCurrentModeType() == Configuration.UI_MODE_TYPE_DESK

---

## Android中的樣式（Theme）是允許被繼承的，如果要繼承某個基本樣式時，需要使用下列<style>標籤中的哪一項屬性？

1. [ ] extend
2. [ ] inherit
3. [x] parent
4. [ ] super

---

## 當在專案內新增一個Android模組（Module）之後，系統全域組態檔內需加入新模組的名稱，下列哪一個檔案正確？

1. [ ] 專案的build.gradle
2. [ ] 模組內的build.gradle
3. [x] 專案的settings.gradle
4. [ ] 專案的local.properties

---

## 關於建立Content Provider的敘述，下列哪一項錯誤？

1. [ ] 自行建立的Content Provider需繼承ContentProvider
2. [x] Content Provider只能給同一個AndroidManifest.xml中所定義的Activity使用
3. [ ] 建立的Content Provider，開發者可以使用自訂的資料存取方式存取資料
4. [ ] Content Provider可以讓某個應用所儲存的資料，被其他應用讀取或寫入

---

## 系統內建的計算機程式屬於Android框架的哪一層？

1. [x] Application層
2. [ ] Application Framework層
3. [ ] Libraries層
4. [ ] Kernel & Driver層

---

## 結束一個Activity時，下列哪一項是正確的生命週期順序？

1. [ ] onCreate → onStart → onResume
2. [ ] onStop → onPause → onDestroy
3. [ ] onDestroy → onPause → onStop
4. [x] onPause → onStop → onDestroy

---

## 關於android:id屬性的敘述，下列哪一項正確？

1. [x] 程式中可以使用對應的id來操作對應的介面元件
2. [ ] 可以用id決定元件顯示順序
3. [ ] 程式中可以用id產生新介面元件
4. [ ] 一個介面元件中可以同時定義多個id

---

## 從一個正在運作的程式切換到另一個程式時，原有Activity會先呼叫下列哪一項方法？

1. [ ] onStop
2. [ ] onDestroy
3. [x] onPause
4. [ ] onRelease

---

## 下列哪些方式是Android系統中所提供，可用來解析XML內容？

1. [x] DOM
2. [ ] XSS
3. [x] SAX
4. [x] PULL

---

## 若要利用Android Support Library程式庫來開發具有ActionBar的程式，模組組態設定檔（build.gradle）需要設定相關性，加入Android Support Library程式庫，下列哪一項正確？（註：xx.y.z會因SDK版本不同而有所不同）

1. [ ] com.google.android.gms:play-services:xx.y.z
2. [x] com.android.support:appcompat-v7:xx.y.z
3. [ ] com.android.support:support-annotations:xx.y.z
4. [ ] com.android.support:recyclerview-v7:xx.y.z

---

## 利用Android Studio開發專案時，若要匯出專案的設定資訊，需要利用下列哪一項功能？

1. [ ] File/Settings
2. [ ] File/Project Structure
3. [ ] File/Import Settings
4. [x] File/Export Settings

---

## 關於[BACK]鍵的敘述，下列哪一項正確？

1. [x] 按下[BACK]鍵會結束目前的Activity並將前一個Activity移到前景
2. [ ] 按下[BACK]鍵會結束目前的Activity並將前一個Activity移到背景
3. [ ] 按下[BACK]鍵會繼續目前的Activity並將前一個Activity移到前景
4. [ ] 按下[BACK]鍵會繼續目前的Activity並將前一個Activity移到背景

---

## 關於Android下SQLite的工具，下列敘述哪一項錯誤？

1. [ ] .tables顯示Table一覽
2. [ ] .scheme顯示Table定義
3. [x] .dump刪除資料庫
4. [ ] sqlite3存取資料庫

---

## 定義一個版面配置檔案，需要重疊顯示多個介面元件時，一般會使用下列哪一項版面配置元件？

1. [ ] LinearLayout
2. [ ] RelativeLayout
3. [ ] TableLayout
4. [x] FrameLayout

---

## 通知點（Notification Dots）是透過浮動圓點的方式來提醒使用者有未讀的通知，下列哪一項方法是顯示或隱藏該圓點的方法？

1. [x] setShowBage(boolean)
2. [ ] setShowDot(boolean)
3. [ ] ShowBage(boolean)
4. [ ] ShowDot(boolean)

---

## 如附圖所示之程式碼，若將附圖之程式碼寫進程式中，會執行下列哪一項動作？

1. [ ] 使用完整路徑呼叫相對應的Intent
2. [x] 開啟瀏覽器瀏覽網頁
3. [ ] 不動作
4. [ ] 開啟Email程式寄送內容資訊

---

## Wear OS使用下列哪一個類別來建立訊息通知（Notification）需要的標題（ContentTitle）、圖示（Icon）及內容（ContentText）等資料？

1. [ ] Notification.Builder
2. [x] NotificationCompat.Builder
3. [ ] WearNotification.Builder
4. [ ] Notification.Create

---

## 將應用程式發佈至Google Play時，下列敘述哪些錯誤？

1. [ ] 如果應用程式需要50MB以上的記憶體空間，可使用擴充程式檔儲存額外的APK資源
2. [ ] 單一APK檔案大小上限為50MB
3. [x] 必須建立Google Play開發人員帳戶，且每年需繳交年費25美元
4. [x] 每個應用程式可多儲存兩個擴充程式檔，兩個檔案的大小上限皆為1GB

---

## 當RecyclerView元件需要一個View來顯示內容選項時，會使用下列哪一個方法來建立需要的View？

1. [ ] onCreateView()
2. [ ] onCreateViewPage()
3. [ ] onNewView()
4. [x] onCreateViewHolder()

---

## 關於Notification訊息的敘述，下列哪一項錯誤？

1. [ ] 可以加入音效提醒
2. [x] 可以加入影片提醒
3. [ ] 可以加入震動提醒
4. [ ] 可以加入閃燈提醒

---

## 關於PreferenceManager.getDefaultSharedPreferences取得Preference的敘述，下列哪些正確？

1. [x] Preferences保存的內容會在/data/data/%Package Name%/shared_prefs內
2. [x] Preferences的檔案名稱為%Package Name%_preferences.xml
3. [ ] 使用此方法時，需傳入檔案名稱及存取模式
4. [ ] Preferences可以任意設定儲存位置

---

## 在Android SDK中，下列哪一項指令可以模擬記憶卡？

1. [ ] ddms
2. [ ] adb
3. [x] mksdcard
4. [ ] draw9patch

---

## 下列哪一個不是共享元素切換（Shared Elements Transition）所支援的動畫效果？

1. [x] changeTextTransform
2. [ ] changeBounds
3. [ ] changeClipBounds
4. [ ] changeTransform

---

## 下列哪一項類別與影音播放無關？

1. [ ] VideoView
2. [ ] MediaPlayer
3. [ ] MediaController
4. [x] WidgetProvider

---

## 在Android中，使用下列哪些物件來解析JSON格式之資料？

1. [x] JSONArray
2. [ ] JSONNode
3. [ ] JSONTree
4. [x] JSONObject

---

## 關於Android指紋辨識技術的敘述，下列哪些正確？

1. [ ] Android 4.3版SDK中新增了指紋辨識API
2. [x] 應用程式中可使用getSystemService (Context.FINGERPRINT _SERVICE)取得FingerprintManager物件
3. [ ] 以FingerprintManager.auth()方法來做指紋掃描認證
4. [x] 需在有支援指紋傳感器的設備上才能正常使用

---

## 當開發一個背景執行的服務模組後，AndroidManifest.xml組態檔應加入下列哪一種標籤，該服務模組方可正常被叫用？

1. [ ] <activity ...>
2. [x] <service ...>
3. [ ] <receiver ...>
4. [ ] <provider ...>

---

## 安裝好Android Studio/Android SDK之後，系統會產生一個開發用的金鑰檔案庫，一般稱為除錯金鑰檔案庫（debug keystore），以利開發、除錯階段使用。請問下列哪一項可能是除錯金鑰檔案庫的位置（Windows環境）？

1. [ ] <專案路徑>\debug.keystore
2. [x] C:\Users\<user>\.android\debug.keystore
3. [ ] <專案路徑>\<模組名稱>\debug.keystore
4. [ ] <JAVA_HOME>\debug.keystore

---

## 使用下列哪一項方式可以取得SharedPreferences的物件？

1. [x] PreferenceManager.getDefaultSharedPreferences(context);
2. [ ] new SharedPreferences();
3. [ ] new SharedPreferencesImpl();
4. [ ] SharedPreferences.getInstance();

---

## 下列哪一項程式，可以將手機鈴聲模式設定為靜音模式？

1. [ ] AudioManager.setRingerMode(AudioManager.RINGER_MODE_NORMAL);
2. [x] AudioManager.setRingerMode(AudioManager.RINGER_MODE_SILENT);
3. [ ] AudioManager.setRingerMode(AudioManager.RINGER_MODE_OFF);
4. [ ] AudioManager.setRingerMode(AudioManager.RINGER_MODE_VIBRATE);

---

## 組態設定中關於App版本的參數，下列敘述哪些正確？

1. [x] versionCode一定是數字，記錄該App發行的版本，主要是提供程式內部與應用程式商城（如：Google Play）辨識App版本之用
2. [ ] versionName一定是數字，記錄該App發行的版本，主要是提供程式內部與應用程式商城（如：Google Play）辨識App版本之用
3. [ ] versionCode可以是文字，記錄該App發行的版本資訊，主要是提供使用者辨識該App的版本
4. [x] versionName可以是文字，記錄該App發行的版本資訊，主要是提供使用者辨識該App的版本

---

## 如附圖所示之程式碼，這段程式碼比較可能出現在下列哪一類Activity中？

1. [ ] 負責呼叫的Activity
2. [x] 被呼叫的Activity
3. [ ] 全新啟動的Activity
4. [ ] Activity報表

---

## Android Things在設備更新策略中，下列哪一項策略是下載並套用可用更新？

1. [ ] POLICY_CHECKS_ONLY
2. [x] POLICY_APPLY_ONLY
3. [ ] POLICY_APPLY_AND_REBOOT
4. [ ] 無此策略

---

## 要開發NFC相關的應用程式時，需要在AndroidManifest.xml設定檔中加入下列哪一項權限設定？

1. [ ] <uses-permission android:name="android.permission.RAED_NFC"/>
2. [x] <uses-permission android:name="android.permission.NFC"/>
3. [ ] <uses-permission android:name="android.permission. ACCESS_NFC"/>
4. [ ] 不需加入任何權限設定

---

## 下列哪些Sensor的類型（Type）在Android 6.0後已不建議被使用（deprecated）？

1. [x] Sensor.TYPE_TEMPERATURE
2. [x] Sensor.TYPE_ORIENTATION
3. [ ] Sensor.TYPE_GYROSCOPE
4. [ ] Sensor.TYPE_ACCELEROMETER

---

## 系統內建的撥電話程式屬於Android框架的哪一層？

1. [x] Application層
2. [ ] Application Framework層
3. [ ] Libraries層
4. [ ] Kernel & Driver層

---

## 關於TDD（Test-Driven-Development）的敘述，下列哪些正確？

1. [ ] TDD為一種單元測試框架
2. [x] 是一種先建立test case，再一步步進行程式撰寫、重構的開發方式
3. [ ] 是一種先寫出完整程式碼再進行測試的方法
4. [x] TDD的規格本身就是可執行文件

---

## Android Studio在下列哪一個版本中加入Instant Run功能？

1. [ ] Android Studio 1
2. [x] Android Studio 2
3. [ ] Android Studio 3
4. [ ] Android Studio 4

---

## RecyclerView元件使用下列哪一個方法，來取得被點擊項目View的位置？

1. [ ] getItemViewType()
2. [x] getAdapterPosition()
3. [ ] getItemPosition()
4. [ ] getViewPosition()

---

## Activity的生命週期中，包括下列哪些方法？

1. [x] onStart
2. [x] onDestroy
3. [x] onStop
4. [x] onCreate

---

## CardView是下列哪一個Layout的子類別？

1. [x] FrameLayout
2. [ ] LinearLayout
3. [ ] RelativeLayout
4. [ ] TableLayout

---

## 當程式內需要存取特殊資源時，必須在清單檔案內（AndroidManifest.xml）宣告；若需要存取網際網路的資源（如：Web Service），請問下列哪一項必須加入清單檔內？

1. [ ] <permission android:name = "android.permission.INTERNET" />
2. [x] <uses-permission android:name = "android.permission.INTERNET" />
3. [ ] <permission android:name = "android.permission.NETWORK" />
4. [ ] <uses-permission android:name = "android.permission.NETWORK" />

---

## 活動（Activity）會因為組態的改變（如：由橫放變成直立），而被摧毀且再重新啟動。通常會藉由覆寫下列哪些方法，將重要資料在活動被摧毀前利用Bundle記錄，並在重新啟動後將資料從Bundle取回？

1. [x] Activity.onSaveInstnaceState(...)
2. [x] Activity.onRestoreInstnaceState(...)
3. [ ] Activity.onStop(...)
4. [ ] Activity.onPause(...)

---

## 下列哪一項不是Context.openFileOutput()方法所支援的讀寫模式？

1. [ ] Context.MODE_PRIVATE
2. [ ] Context.MODE_APPEND
3. [ ] Context.MODE_WORLD_WRITEABLE
4. [x] Context.MODE_PROTECTED

---

## 新啟動一個Activity時，會最先呼叫下列哪一項方法？

1. [ ] onStart
2. [ ] onOpen
3. [ ] onStartCommand
4. [x] onCreate

---

## 在Activity裡配置SurfaceView，並利用SurfaceView播放影片，需要使Activity實作下列哪一項介面？

1. [ ] SurfaceHolder
2. [ ] SurfaceView
3. [x] SurfaceHolder.Callback
4. [ ] SurfaceView.Callback

---

## 使用Doze模式雖然可減少電池的電量消耗，但有可能會影響推播機制的功能，可用下列哪些方法來解決？

1. [x] 增加應用程式到電池最佳化的白名單列表（Whitelist）中
2. [x] 使用ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS和REQUEST_IGNORE_BATTERY_OPTIMIZATIONS設定忽略電池最佳化
3. [ ] 使用其它廠商的推播機制
4. [ ] 無方法可解決

---

## 下列哪一項為用來階層化設定項目，點選後會跳至下一個設定畫面的元件？

1. [ ] PreferenceCategory
2. [x] PreferenceScreen
3. [ ] RingtonePreference
4. [ ] ListPreference

---

## Android API level27引進了一個新的SharedMemory類別，主要是用來加強多個執行緒（Thread）或應用程式溝通之用，他提供開發者可以對匿名共用記憶體進行下列哪些操作？

1. [x] 創建
2. [x] 映射
3. [ ] 編輯
4. [ ] 搬移

---

## 關於Android下的資料庫存取API，下列敘述哪些錯誤？

1. [x] 不能使用transaction
2. [x] 不支援in memory database
3. [x] 不支援pre-compiled statement
4. [x] 透過API存取資料庫只能新增資料，不能刪除

---

## Paint物件可以在Canvas上繪製圖形，假設目前程式中有一個Canvas物件名稱為canvas，一個Paint物件名稱為paint，請問要如何在Canvas上畫一個圓形，其圓心座標為x:40、y:40，直徑為50？

1. [ ] canvas.drawCircle(40,40,50,paint);
2. [x] canvas.drawCircle(40,40,25,paint);
3. [ ] canvas.drawRect(40,40,50,paint);
4. [ ] canvas.drawOval(40,40,25,paint);

---

## 要使用指紋辨識來驗證使用者的身分時，需要在AndroidManifest.xml設定檔中加入下列哪一項權限設定？

1. [ ] ^[GD90601]
2. [ ] ^[GD90602]
3. [x] ^[GD90603]
4. [ ] ^[GD90604]

---

## 在Android應用程式中要實作錄音的功能，除了在AndroidManifest.xml設定檔加入android.permission.WRITE_EXTERNAL_STORAGE的權限外，還需加入下列哪一項權限設定？

1. [ ] <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
2. [ ] <uses-permission android:name="android.permission.REORDER_TASKS"/>
3. [x] <uses-permission android:name="android.permission.RECORD_AUDIO"/>
4. [ ] 不需加入任何權限設定

---

## 關於Android下SQLite的工具，下列敘述哪一項錯誤？

1. [ ] .tables顯示Table一覽
2. [ ] .scheme顯示Table定義
3. [ ] 以SELECT取得資料
4. [x] view tables顯示Table一覽

---

## 在程式中若直接呼叫finish函式來關閉Activity，下列哪一項是正確的生命週期順序？

1. [ ] onCreate → onStart → onResume
2. [ ] onStop → onResume → onCreate
3. [ ] onResume → onStart → onOpen
4. [x] onPause → onStop → onDestroy

---

## 利用數位簽章（金鑰）進行簽章過的APK檔案（Singed APK）方能上傳到Google Play。當我們設定數位簽章參數時需要輸入兩種密碼：金鑰檔案庫密碼以及金鑰密碼；而這些參數在設定後會儲存在模組內的組態設定檔（build.gradle）。請問下列哪一個參數代表「金鑰檔案庫密碼」？

1. [ ] keyAlias
2. [ ] keyPassword
3. [ ] storeFile
4. [x] storePassword

---

## Android系統未內建支援下列哪一項聲音檔案格式？

1. [x] .rm
2. [ ] .mp3
3. [ ] .aac
4. [ ] .mid

---

## 下列哪一項會出現在Android應用程式上方標題列、桌面圖示下方的名稱？

1. [x] Application name
2. [ ] Company Domain
3. [ ] Package name
4. [ ] Project location

---

## 關於RelativeLayout版面配置元件的敘述，下列哪一項錯誤？

1. [ ] RelativeLayout元件中，可以包含多層版面
2. [ ] 一個畫面中可以包含多個RelativeLayout元件
3. [ ] 在一個RelativeLayout元件中，可以同時定義直向與橫向顯示內容元件
4. [x] 在RelativeLayout元件中，可以透過android:orientation屬性定義內容元件排列方向

---

## 若要將Android範例程式匯入至Android Studio，需點選啟動畫面中的哪一個項目？

1. [ ] Start a new Android Studio project
2. [ ] Open an existing Android Studio project
3. [x] Import an Android code sample
4. [ ] Import project(Eclipse ADT, Gradle, etc)

---

## 系統內建的TelephonyManager屬於Android框架的哪一層？

1. [ ] Application層
2. [x] Application Framework層
3. [ ] Libraries層
4. [ ] Kernel & Driver層

---

## 利用Gradle設定組態定義檔時，可以指定相關性（Dependencies）的程式庫或模組。請問目前支援下列哪些相關性設定方式？

1. [x] File Dependencies
2. [x] Library Dependencies
3. [x] Module Dependencies
4. [ ] Directory Dependencies

---

## 下列哪一個專案檔案內，儲存了Android SDK開發工具的路徑資訊？

1. [ ] 專案的build.gradle
2. [ ] 模組內的build.gradle
3. [ ] 專案的settings.gradle
4. [x] 專案的local.properties

---

## 若要維持main.xml作為介面設計，且程式內採「R.layout.main」方式讀取，撰寫一個橫式與直式顯示介面不同的應用程式，可以在專案res目錄中加入下列哪些目錄來達成？

1. [ ] values-land目錄
2. [x] layout-land目錄
3. [x] layout目錄
4. [ ] values-port目錄

---

## 當新增了一個提供使用者登入的新活動（Activity），組態檔AndroidManifest.xml必須加入該活動的宣告標籤<activity>；若新活動的類別名稱為LoginActivity，請問必須利用下列activity的哪一個屬性來指定此活動的類別名稱？

1. [x] android:name = "LoginActivity"
2. [ ] android:label = "LoginActivity"
3. [ ] android:class = "LoginActivity"
4. [ ] android:activity = "LoginActivity"

---

## 下列哪一項是MediaPlayer類別的功能？

1. [x] 播放音樂
2. [ ] 預覽圖片
3. [ ] 顯示網頁
4. [ ] 撥打電話

---

## 下列哪一個語法可以得知外部儲存（External Storage）的狀態？

1. [x] Environment.getExternalStorageState()
2. [ ] Environment.getExternalStorageDirectory()
3. [ ] Environment.getExternalStoragePublicDirectory()
4. [ ] Environment.getExternalStorage()

---

## 關於AndroidManifest.xml檔案的功用，下列哪些正確？

1. [x] 註冊該程式所要提供的功能
2. [x] 列出該程式所需要的權限
3. [x] 指定使用到的函式庫
4. [ ] 指定使用到的資源檔案路徑

---

## 下列哪一項是Activity類別的作用？

1. [x] 顯示可見的使用者介面
2. [ ] 提供可呼叫的資料
3. [ ] 提供網路連線
4. [ ] 提供不影響正常使用流程的訊息提示

---

## 在應用程式中建立SensorEventListener物件，下列哪些方法一定要被覆寫（override）？

1. [ ] onLocationChanged()
2. [ ] onSensorDisabled()
3. [x] onAccuracyChanged()
4. [x] onSensorChanged()

---

## 如附圖所示之程式碼，是有關APP Links的intent-filter設定，缺少了下列哪一項必要的屬性？

1. [ ] <category android:name="android.intent.category.DEFAULT" />
2. [x] <category android:name="android.intent.category.BROWSABLE" />
3. [ ] <data android:scheme="https" android:host="www.abc.com" />
4. [ ] <action android:name="android.intent.action.VIEW" />

---

## 在Activity運作中接到一通來電，Activity會最先呼叫下列哪一項方法？

1. [ ] onCall
2. [ ] onStop
3. [ ] onStopCommand
4. [x] onPause

---

## 關於Notification訊息的敘述，下列哪些正確？

1. [x] 提供了一個不影響正常操作的訊息提示方式
2. [ ] 使用者需中斷目前工作先處理Notification訊息
3. [x] Notification訊息欄控制音樂播放
4. [ ] Notification訊息欄中的訊息僅能閱讀，無法進行App啟動或刪除通知

---

## LocationListener可以協助我們在Google Map應用程式中，即時追蹤GPS座標狀態，其中下列哪一項方法可以即時捕捉座標改變的事件？

1. [x] onLocationChanged(Location location)
2. [ ] onStatusChanged(String provider, int status, Bundle extras)
3. [ ] onProviderEnabled(String provider)
4. [ ] onProviderDisabled(String provider)

---

## 當程式使用了Fragment進行功能開發，為了讓該程式可以在Android 1.6至Android 5.1之間的手機都能夠正常執行，需要引用下列哪一個程式庫？

1. [x] v4 support library
2. [ ] v7 cardview library
3. [ ] v7 gridlayout library
4. [ ] v14 preference support library

---

## 關於Context的SQLite相關API之敘述，下列哪些正確？

1. [x] 可以在繼承Context的類別中使用openOrCreateDatabase開啟資料庫
2. [x] 如果使用openOrCreateDatabase時，指定的資料庫不存在，則會丟出錯誤
3. [x] Context有deleteDatabase方法來刪除Database
4. [ ] 可以使用openOrCreateDatabase來取得其他應用程式的private Database

---

## 在Wear OS製作訊息顯示服務時，預設情況下，訊息通知會從配對的手機上的應用程式橋接（共享）至手錶，有可能會造成重覆訊息的建立，下列哪些設定可以處理此問題？

1. [x] 在AndroidManifest.xml中指定橋接模式（Bridging mode）設定
2. [x] 在程式執行過程中指定橋接模式（Bridging mode）設定
3. [x] 設定dismissal ID，關閉通知同步到所有設備
4. [ ] 無法解決此問題

---

## 下列哪一個Android專案檔案，可以用來讓使用者加入自行定義的樣式設定？

1. [ ] res/values/dimens.xml
2. [ ] res/values/color.xml
3. [ ] res/values/strings.xml
4. [x] res/values/styles.xml

---

## 下列哪一項不是預設的Preference元件？

1. [ ] CheckBoxPreference
2. [ ] RingtonePreference
3. [ ] ListPreference
4. [x] SelectionPreference

---

## 如果要在Android TV應用程式中建置媒體目錄瀏覽介面，建議增加下列哪些函式庫（library）？

1. [x] v17 leanback library
2. [x] v7 recyclerview library
3. [x] v7 cardview library
4. [ ] v7 mediarouter library

---

## 下列哪些選項為開發NFC應用程式時，可支援的BroadcastReceiver廣播訊息事件？

1. [x] NfcAdapter.ACTION_TECH_DISCOVERED
2. [x] NfcAdapter.ACTION_ADAPTER_STATE_CHANGED
3. [x] NfcAdapter.ACTION_NDEF_DISCOVERED
4. [x] NfcAdapter.ACTION_TAG_DISCOVERED

---

## 寫一支新增聯絡人的程式時，在AndroidManifest.xml需加入下列哪一項權限？

1. [ ] android.permission.WRITE_CALENDAR
2. [ ] android.permission.WRITE_OWNER_DATA
3. [ ] android.permission.READ_CONTACTS
4. [x] android.permission.WRITE_CONTACTS

---

## 如果要在指定的按鈕或是Menu Item被按下時啟動Search，該使用下列哪一項API？

1. [x] Activity#onSearchRequested
2. [ ] Activity#createSearch
3. [ ] Activity#makeNewSearch
4. [ ] 無法讓按鈕或Menu Item按下後便啟動Search

---

## 某一應用程式在使用Context#openFileOutput("test.txt",MODE_PRIVATE)並正確寫入資料後，下列哪些方式可正確讀取檔案？

1. [x] 透過Context#openFileInput("test.txt")
2. [x] 透過new FileReader("/data/data/" + this.getPackageName() + "/files/data.txt")
3. [ ] 透過new FileReader("test.txt")
4. [ ] 透過new File("test.txt")

---

## 關於Android中提供的SQLite資料庫相關API之敘述，下列哪一項錯誤？

1. [ ] SQLiteOpenHelper用來輔助建立或變更資料庫
2. [ ] SQLiteDatabase提供新增資料，刪除資料和執行SQL命令…等方法
3. [x] CursorValues儲存一組資料用來更新或刪除資料使用
4. [ ] SQLiteCursor為實作Cursor用來取出查詢結果使用

---

## PhoneStateListener為取得電話的通話狀態，下列敘述哪一項錯誤？

1. [ ] 配合android.permission.READ_PHONE_STATE權限存取通話狀態
2. [x] 在PhoneStateListener裡覆寫onDataActivity()方法取得通話狀態
3. [ ] 若對方有設定來電顯示號碼，透過PhoneStateListener可取得來電的電話號碼
4. [ ] TelephonyManager.CALL_STATE_IDLE為處理待機狀態的事件

---
